<nav class="navbar navbar-expand-md navbar-light bg-light">
  <%= link_to 'Forum', root_path, class: 'navbar-brand' %>
  <button type="button" class="navbar-toggler" data-toggle="collapse" data-target="#navbarCollapse">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse justify-content-between" id="bs-example-navbar-collapse-1">
    

      <%= form_tag root_path, method: :get, class: 'form-inline my-2 my-lg-0' do %>
      <div class="form-group">
        <%= text_field_tag :search, nil, class: 'form-control' %>
        <%= submit_tag "Cari", class: 'btn btn-outline-secondary' %>
      </div>
      <% end %>
      <div class="navbar-nav">
        <a href="#" class="nav-item nav-link">Link</a>
        <div class="nav-item dropdown">
          <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= user_signed_in? ? current_user.name : 'Halo Guest' %><span class="caret"></span></a>
          <ul class="dropdown-menu">
            <% if user_signed_in? %>
            <div> <li><%= link_to 'Logout', destroy_user_session_path, method: :delete %></li></div>
            <% else %>
            <li><%= link_to 'Daftar', new_user_registration_path %></li>
            <li><%= link_to 'Login', new_user_session_path %></li>
            <% end %>
          </ul>
        </div>
      </div>
    </div>
  </nav>